* `carthage version`: 
* `xcodebuild -version`: 
* Are you using `--no-build`?
* Are you using `--no-use-binaries`?
* Are you using `--use-submodules`?

**Cartfile**
```
<YOUR CARTFILE>
```

**Carthage Output**
```
<OUTPUT>
```
